# Выполнить один раз в PowerShell, находясь в папке проекта:
# C:\Users\Kampuktr\Desktop\Websites\website1
#
# Проверил: .gitignore у тебя уже есть и он нормальный (исключает vendor,
# node_modules, .env, public/build и т.д.) — пересоздавать не нужно.
# Репозиторий на GitHub пустой, так что конфликтов не будет.

git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/790azat/website1.git
git push -u origin main

# На последней команде (git push) откроется окно браузера — это Git Credential
# Manager, войди там в свой аккаунт GitHub (790azat). Пароль/токен вводить в
# чат мне не нужно и не присылай — вся авторизация проходит у тебя в браузере.
#
# Если git вообще не установлен, команда "git init" покажет ошибку
# "git is not recognized" — тогда сначала поставь Git for Windows:
# https://git-scm.com/download/win
